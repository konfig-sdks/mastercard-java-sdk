/*
 * Open Banking
 * OpenAPI specification for Finicity APIs.  Open Banking solutions in the US are provided by Finicity, a Mastercard company.
 *
 * The version of the OpenAPI document: 1.16.0
 * Contact: apisupport@mastercard.com
 *
 * NOTE: This class is auto generated by Konfig (https://konfigthis.com).
 * Do not edit the class manually.
 */


package com.konfigthis.client.model;

import java.util.Objects;
import java.util.Arrays;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.konfigthis.client.model.StateAttribute;
import com.konfigthis.client.model.StreamModel;
import com.konfigthis.client.model.TransactionalAttribute;
import io.swagger.annotations.ApiModel;
import io.swagger.annotations.ApiModelProperty;
import java.io.IOException;
import java.util.ArrayList;
import java.util.List;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import org.apache.commons.lang3.StringUtils;

import java.lang.reflect.Type;
import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Map.Entry;
import java.util.Set;

import com.konfigthis.client.JSON;

/**
 * Analytics calculated for one account in the report.
 */
@ApiModel(description = "Analytics calculated for one account in the report.")@javax.annotation.Generated(value = "Generated by https://konfigthis.com")
public class AccountAnalytics {
  public static final String SERIALIZED_NAME_TRANSACTIONAL_ATTRIBUTES = "transactionalAttributes";
  @SerializedName(SERIALIZED_NAME_TRANSACTIONAL_ATTRIBUTES)
  private List<TransactionalAttribute> transactionalAttributes = new ArrayList<>();

  public static final String SERIALIZED_NAME_STATE_ATTRIBUTES = "stateAttributes";
  @SerializedName(SERIALIZED_NAME_STATE_ATTRIBUTES)
  private List<StateAttribute> stateAttributes = new ArrayList<>();

  public static final String SERIALIZED_NAME_STREAMS = "streams";
  @SerializedName(SERIALIZED_NAME_STREAMS)
  private List<StreamModel> streams = new ArrayList<>();

  public AccountAnalytics() {
  }

  public AccountAnalytics transactionalAttributes(List<TransactionalAttribute> transactionalAttributes) {
    
    
    
    
    this.transactionalAttributes = transactionalAttributes;
    return this;
  }

  public AccountAnalytics addTransactionalAttributesItem(TransactionalAttribute transactionalAttributesItem) {
    this.transactionalAttributes.add(transactionalAttributesItem);
    return this;
  }

   /**
   * List of calculated transactional attributes
   * @return transactionalAttributes
  **/
  @javax.annotation.Nonnull
  @ApiModelProperty(required = true, value = "List of calculated transactional attributes")

  public List<TransactionalAttribute> getTransactionalAttributes() {
    return transactionalAttributes;
  }


  public void setTransactionalAttributes(List<TransactionalAttribute> transactionalAttributes) {
    
    
    
    this.transactionalAttributes = transactionalAttributes;
  }


  public AccountAnalytics stateAttributes(List<StateAttribute> stateAttributes) {
    
    
    
    
    this.stateAttributes = stateAttributes;
    return this;
  }

  public AccountAnalytics addStateAttributesItem(StateAttribute stateAttributesItem) {
    this.stateAttributes.add(stateAttributesItem);
    return this;
  }

   /**
   * List of calculated state attributes
   * @return stateAttributes
  **/
  @javax.annotation.Nonnull
  @ApiModelProperty(required = true, value = "List of calculated state attributes")

  public List<StateAttribute> getStateAttributes() {
    return stateAttributes;
  }


  public void setStateAttributes(List<StateAttribute> stateAttributes) {
    
    
    
    this.stateAttributes = stateAttributes;
  }


  public AccountAnalytics streams(List<StreamModel> streams) {
    
    
    
    
    this.streams = streams;
    return this;
  }

  public AccountAnalytics addStreamsItem(StreamModel streamsItem) {
    this.streams.add(streamsItem);
    return this;
  }

   /**
   * List of generated streams
   * @return streams
  **/
  @javax.annotation.Nonnull
  @ApiModelProperty(required = true, value = "List of generated streams")

  public List<StreamModel> getStreams() {
    return streams;
  }


  public void setStreams(List<StreamModel> streams) {
    
    
    
    this.streams = streams;
  }

  /**
   * A container for additional, undeclared properties.
   * This is a holder for any undeclared properties as specified with
   * the 'additionalProperties' keyword in the OAS document.
   */
  private Map<String, Object> additionalProperties;

  /**
   * Set the additional (undeclared) property with the specified name and value.
   * If the property does not already exist, create it otherwise replace it.
   *
   * @param key name of the property
   * @param value value of the property
   * @return the AccountAnalytics instance itself
   */
  public AccountAnalytics putAdditionalProperty(String key, Object value) {
    if (this.additionalProperties == null) {
        this.additionalProperties = new HashMap<String, Object>();
    }
    this.additionalProperties.put(key, value);
    return this;
  }

  /**
   * Return the additional (undeclared) property.
   *
   * @return a map of objects
   */
  public Map<String, Object> getAdditionalProperties() {
    return additionalProperties;
  }

  /**
   * Return the additional (undeclared) property with the specified name.
   *
   * @param key name of the property
   * @return an object
   */
  public Object getAdditionalProperty(String key) {
    if (this.additionalProperties == null) {
        return null;
    }
    return this.additionalProperties.get(key);
  }


  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    AccountAnalytics accountAnalytics = (AccountAnalytics) o;
    return Objects.equals(this.transactionalAttributes, accountAnalytics.transactionalAttributes) &&
        Objects.equals(this.stateAttributes, accountAnalytics.stateAttributes) &&
        Objects.equals(this.streams, accountAnalytics.streams)&&
        Objects.equals(this.additionalProperties, accountAnalytics.additionalProperties);
  }

  @Override
  public int hashCode() {
    return Objects.hash(transactionalAttributes, stateAttributes, streams, additionalProperties);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class AccountAnalytics {\n");
    sb.append("    transactionalAttributes: ").append(toIndentedString(transactionalAttributes)).append("\n");
    sb.append("    stateAttributes: ").append(toIndentedString(stateAttributes)).append("\n");
    sb.append("    streams: ").append(toIndentedString(streams)).append("\n");
    sb.append("    additionalProperties: ").append(toIndentedString(additionalProperties)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("transactionalAttributes");
    openapiFields.add("stateAttributes");
    openapiFields.add("streams");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
    openapiRequiredFields.add("transactionalAttributes");
    openapiRequiredFields.add("stateAttributes");
    openapiRequiredFields.add("streams");
  }

 /**
  * Validates the JSON Object and throws an exception if issues found
  *
  * @param jsonObj JSON Object
  * @throws IOException if the JSON Object is invalid with respect to AccountAnalytics
  */
  public static void validateJsonObject(JsonObject jsonObj) throws IOException {
      if (jsonObj == null) {
        if (!AccountAnalytics.openapiRequiredFields.isEmpty()) { // has required fields but JSON object is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in AccountAnalytics is not found in the empty JSON string", AccountAnalytics.openapiRequiredFields.toString()));
        }
      }

      // check to make sure all required properties/fields are present in the JSON string
      for (String requiredField : AccountAnalytics.openapiRequiredFields) {
        if (jsonObj.get(requiredField) == null) {
          throw new IllegalArgumentException(String.format("The required field `%s` is not found in the JSON string: %s", requiredField, jsonObj.toString()));
        }
      }
      // ensure the json data is an array
      if (!jsonObj.get("transactionalAttributes").isJsonArray()) {
        throw new IllegalArgumentException(String.format("Expected the field `transactionalAttributes` to be an array in the JSON string but got `%s`", jsonObj.get("transactionalAttributes").toString()));
      }

      JsonArray jsonArraytransactionalAttributes = jsonObj.getAsJsonArray("transactionalAttributes");
      // validate the required field `transactionalAttributes` (array)
      for (int i = 0; i < jsonArraytransactionalAttributes.size(); i++) {
        TransactionalAttribute.validateJsonObject(jsonArraytransactionalAttributes.get(i).getAsJsonObject());
      };
      // ensure the json data is an array
      if (!jsonObj.get("stateAttributes").isJsonArray()) {
        throw new IllegalArgumentException(String.format("Expected the field `stateAttributes` to be an array in the JSON string but got `%s`", jsonObj.get("stateAttributes").toString()));
      }

      JsonArray jsonArraystateAttributes = jsonObj.getAsJsonArray("stateAttributes");
      // validate the required field `stateAttributes` (array)
      for (int i = 0; i < jsonArraystateAttributes.size(); i++) {
        StateAttribute.validateJsonObject(jsonArraystateAttributes.get(i).getAsJsonObject());
      };
      // ensure the json data is an array
      if (!jsonObj.get("streams").isJsonArray()) {
        throw new IllegalArgumentException(String.format("Expected the field `streams` to be an array in the JSON string but got `%s`", jsonObj.get("streams").toString()));
      }

      JsonArray jsonArraystreams = jsonObj.getAsJsonArray("streams");
      // validate the required field `streams` (array)
      for (int i = 0; i < jsonArraystreams.size(); i++) {
        StreamModel.validateJsonObject(jsonArraystreams.get(i).getAsJsonObject());
      };
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!AccountAnalytics.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'AccountAnalytics' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<AccountAnalytics> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(AccountAnalytics.class));

       return (TypeAdapter<T>) new TypeAdapter<AccountAnalytics>() {
           @Override
           public void write(JsonWriter out, AccountAnalytics value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             obj.remove("additionalProperties");
             // serialize additonal properties
             if (value.getAdditionalProperties() != null) {
               for (Map.Entry<String, Object> entry : value.getAdditionalProperties().entrySet()) {
                 if (entry.getValue() instanceof String)
                   obj.addProperty(entry.getKey(), (String) entry.getValue());
                 else if (entry.getValue() instanceof Number)
                   obj.addProperty(entry.getKey(), (Number) entry.getValue());
                 else if (entry.getValue() instanceof Boolean)
                   obj.addProperty(entry.getKey(), (Boolean) entry.getValue());
                 else if (entry.getValue() instanceof Character)
                   obj.addProperty(entry.getKey(), (Character) entry.getValue());
                 else {
                   obj.add(entry.getKey(), gson.toJsonTree(entry.getValue()).getAsJsonObject());
                 }
               }
             }
             elementAdapter.write(out, obj);
           }

           @Override
           public AccountAnalytics read(JsonReader in) throws IOException {
             JsonObject jsonObj = elementAdapter.read(in).getAsJsonObject();
             validateJsonObject(jsonObj);
             // store additional fields in the deserialized instance
             AccountAnalytics instance = thisAdapter.fromJsonTree(jsonObj);
             for (Map.Entry<String, JsonElement> entry : jsonObj.entrySet()) {
               if (!openapiFields.contains(entry.getKey())) {
                 if (entry.getValue().isJsonPrimitive()) { // primitive type
                   if (entry.getValue().getAsJsonPrimitive().isString())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsString());
                   else if (entry.getValue().getAsJsonPrimitive().isNumber())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsNumber());
                   else if (entry.getValue().getAsJsonPrimitive().isBoolean())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsBoolean());
                   else
                     throw new IllegalArgumentException(String.format("The field `%s` has unknown primitive type. Value: %s", entry.getKey(), entry.getValue().toString()));
                 } else if (entry.getValue().isJsonArray()) {
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), List.class));
                 } else { // JSON object
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), HashMap.class));
                 }
               }
             }
             return instance;
           }

       }.nullSafe();
    }
  }

 /**
  * Create an instance of AccountAnalytics given an JSON string
  *
  * @param jsonString JSON string
  * @return An instance of AccountAnalytics
  * @throws IOException if the JSON string is invalid with respect to AccountAnalytics
  */
  public static AccountAnalytics fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, AccountAnalytics.class);
  }

 /**
  * Convert an instance of AccountAnalytics to an JSON string
  *
  * @return JSON string
  */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

